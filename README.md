Project built ML model (
Ridge regression with TF-IDF) to predict math problem answers from tiny dataset (10 samples). 
Loaded data, EDA, tuned alpha=100 via GridSearchCV/LOOCV. Poor perf: MSE ~265M, R2 -0.01 due to insufficient data. 
Future: more data, BERT embeddings, other models. (278 chars)

Sources
